// Lightweight highlight.js bundle with selected languages (mirrors markdown-nice)
import hljs from 'highlight.js'

// Using the full highlight.js bundle which includes common languages by default
// This avoids the complex individual language imports that were causing module resolution issues

export default hljs

